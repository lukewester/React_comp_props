

export function MyComponent(props) {
    //    const name = string;
    //    const age = number;
    const {name , age } = props
    return (
        <div>{age} {name}</div>
    )
};




export default MyComponent